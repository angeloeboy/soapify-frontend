import DashboardLayout from "@/components/dashboardLayout";
import TopBar from "@/components/topbar";

let AddProducts = () => {
	return (
		<DashboardLayout>
			<TopBar pageName="Add Products" />
		</DashboardLayout>
	);
};

export default AddProducts;
